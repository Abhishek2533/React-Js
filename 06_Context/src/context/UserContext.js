// this file is pure js

import React from "react";

// create context -> global
const UserContext = React.createContext()
// now we have to make this provider for global -> new file

export default UserContext;